import { Button } from '@/components/ui/button';
import { InfoAlert, WarningAlert, DangerAlert } from '@/components/docs-alert';
import { Code } from '@/components/code';

# Welcome to the About Page

_This is some muted text for subtle emphasis._

This is an example of an MDX file. You can write **Markdown** as usual, and also use React components:

<Button>Click Me!</Button>

- MDX lets you mix JSX and Markdown.
- You can import and use your own components.
- It's great for documentation and interactive content.

> This is a blockquote in MDX. You can use it to highlight important information.

here is an alert:

<InfoAlert>This is an info alert!</InfoAlert>

---

<WarningAlert>This is a warning alert!</WarningAlert>

---

<DangerAlert>This is a danger alert!</DangerAlert>

I have some list here:

1. Item 1
1. Item 2
1. Item 3

<Code language="php">{`
<?php

use App\Http\Controllers\UserController;

Route::get('/users', [UserController::class, 'index']);

`}</Code>

<Code language="typescript">{`
import { QueryBuilder } from '@/components/ui/query-builder';

type QueryBuilderExample = {
    filter: (field: string, value: string) => QueryBuilderExample;
    sort: (field: string) => QueryBuilderExample;
    include: (...relations: string[]) => QueryBuilderExample;
    page: (pageNumber: number) => QueryBuilderExample;
    perPage: (itemsPerPage: number) => QueryBuilderExample;
    build: () => string;
};

# no problemo

const qb = QueryBuilder.make('/api/users')
    .filter('name', 'John')
    .sort('created_at')
    .include('posts', 'comments')
    .page(2)
    .perPage(15);

`}</Code>
